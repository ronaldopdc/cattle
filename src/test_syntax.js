const fs = require('fs'); const html = fs.readFileSync('cash_flow_report_modal.html', 'utf8'); const scriptMatches = html.match(/<script>([\s\S]*?)<\/script>/g); if(scriptMatches) { scriptMatches.forEach((script, i) => { const code = script.replace(/<\/?script>/g, ''); try { new Function(code); console.log('Script ' + i + ' is valid'); } catch (e) { console.error('Script ' + i + ' Error:', e); } }); } else { console.log('No scripts found'); }
